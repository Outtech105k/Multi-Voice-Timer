# タイマーアプリ（複数タイマー対応・読み上げ機能付）実装計画

Cloudflare Pages で公開可能な、レスポンシブ対応かつプレミアムなデザインのタイマーウェブアプリケーションの実装計画です。

## 1. 技術スタック・構成
*   **フレームワーク**: **React + TypeScript + Vite**
    *   状態管理（複数のタイマーの追加、削除、個々のカウントダウン、音声再生制御）が容易に行えるため。
*   **スタイリング**: **Vanilla CSS**
    *   カスタムプロパティ（CSS変数）、グラデーション、グラスモフィズム、マイクロアニメーションを用いた高級感のあるダークモードテーマを構築します。
*   **音声合成**: **Web Speech API (`speechSynthesis`)**
    *   外部サービスやライブラリに依存せず、ブラウザ標準 of TTSで「〇〇（ラベル名）、残り〇分前」の読み上げを実現します。
*   **デプロイ**: **Cloudflare Pages**
    *   Viteによるビルド生成物 (`dist` ディレクトリ) をホスト。設定は標準の静的デプロイメントに対応。

---

## 2. 要件に対する設計

### ① Cloudflare Pages での公開
*   Viteの設定でアセットパスを適切に処理し、`npm run build` で生成される `dist` ディレクトリをデプロイ対象にします。

### ② 複数タイマーの開始・実行
*   各タイマーの状態（ID、ラベル、初期秒数、残り秒数、ステータス `running|paused|completed`、予告再生フラグ）を配列で管理。
*   `requestAnimationFrame` または `setInterval` を用いた高精度タイマーフック（`useTimer` またはグローバルマネージャー）で毎秒更新。
*   ブラウザのタブがバックグラウンドになっても極力ズレないよう、開始時刻と現在時刻の差分（`Date.now()`）から残り時間を算出。

### ③ レスポンシブ対応 & プレミアムデザイン
*   **デザインシステム (CSS Variables)**: ディープネイビーやスレートをベースに、ネオングリーンやシアンをアクセントにした洗練されたダークテーマ。
*   **レイアウト**: モバイルファーストの Grid/Flexbox レイアウト。
*   **要素**:
    *   ガラス調カード（ガラスモフィズム）
    *   タイマーの残り時間を表す円形プログレスバー (SVG)
    *   スムーズなホバーアニメーション

### ④ 予告（プレビュー）機能
*   タイマーがカウントダウンする中で、以下のタイミングで一度だけボイス通知をトリガーします：
    *   **全体時間が60分を超える場合**: 残り30分時点、および残り10分時点。
    *   **全体時間が60分以下の場合**: 残り10分時点。
*   一度読み上げたら重複して呼ばれないよう、各タイマーオブジェクトにフラグ（`voiced30Min`, `voiced10Min`）を持たせます。

### ⑤ タイマーのラベル設定
*   タイマー作成時に「パスタを茹でる」「ミーティング準備」などの任意のラベルをテキスト入力可能。デフォルト値も用意。

### ⑥ 読み上げ機能（TTS）
*   Web Speech API を使用して、指定のタイミングで以下のように読み上げます。
    *   例：「[ラベル名]、残り[30/10]分です」
    *   タイマー終了時：「[ラベル名]が終了しました」

---

## 3. 実装ステップ

### Step 1: プロジェクトの初期化と構成
1.  Gitブランチ `feature/timer-app` を作成。
2.  `npx -y create-vite@latest ./ --template react-ts` を使用してプロジェクトをカレントディレクトリに初期化。
3.  不要な初期ファイルをクリーンアップし、CSSリセットと共通デザインシステムを設定。

### Step 2: タイマーロジックとWeb Speechフックの作成
1.  高精度でバックグラウンドでもズレないタイマーロジックの設計。
2.  音声読み上げを行う `speechService.ts` ユーティリティの作成。

### Step 3: コンポーネントの実装
1.  `TimerForm`: 新規タイマー作成フォーム（ラベル、時間入力）。
2.  `TimerCard`: タイマー個別の表示（SVG円形プログレス、デジタル時計、再生/一時停止/削除ボタン）。
3.  `Dashboard`: タイマー一覧、全停止、履歴や合計時間などの表示。

### Step 4: デザインとインタラクションの磨き込み
1.  Vanilla CSSによる美しいビジュアルとアニメーションの調整。
2.  レスポンシブ表示の最適化。

### Step 5: デプロイ準備
1.  Cloudflare Pages 向けのデプロイ設定確認、ビルドテスト。

---

## 4. フィードバックのご確認

この方針で進めてよろしければ、**Gitブランチの作成とプロジェクトの初期化**から開始いたします。
何か変更・追加したい要件や、デザインに関するご要望はございますか？
